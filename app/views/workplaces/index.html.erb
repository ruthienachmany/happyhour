<h1>Workplaces</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @workplaces.each do |workplace| workplace.name %>
  <tr><td><%= workplace.name %> 
    <%= link_to 'Show', workplace %> 
    <%= link_to 'Edit', edit_user_path(workplace) %>
    <%= link_to 'Destroy', workplace, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_workplace_path %>
